machine:
  python:
    version: 2.7.10
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"

  override: []

checkout:
  post:
    - pip install tests/requirements.yml

test:
  override:
    - pre-commit install
    - pre-commit run --all-files
    - molecule test --driver docker
